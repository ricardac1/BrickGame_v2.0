{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug with Terminal",
            "program": "${workspaceFolder}/src/build/BrickGame_v2.0_cli",
            "args": [],
            "cwd": "${workspaceFolder}/src/build",
            "terminal": "integrated",
            "env": {
                "TERM": "xterm-256color"
            }
        }
    ]
}